export interface CreateCarAddressDto {
  city: string;
  state: string;
  address: string;
  zipCode: string;
}

export interface CreateCarAddressDbDto {
    city: string;
    state: string;
    address: string;
    zip_code: string;
  }
